
export function login(email: string, password: string): boolean {
  // Placeholder: only allow the admin user for now
  // Will be replaced with real authentication logic after backend integration
  console.log("Authenticating user:", email)
  console.log("Password:", password)
  return true
}